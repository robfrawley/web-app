"http://{default}/":
    type: upstream
    upstream: "webapp:http"
    cache:
        enabled: false
    ssi:
        enabled: false

"http://www.{default}/":
    type: redirect
    to: "http://{default}/"